# Legacy compiler fixtures

These are frozen `.aihu` inputs for compiler regressions. They are not maintained
application examples. Their application counterparts remain in the Aihu framework
repository; change a fixture here only with the compiler test that requires it.
